# PR evidence

Screenshot evidence referenced by PR comments across KaveraAI repos.
Layout: `<repo>/<pr-number>/<name>.png`. Written by `~/.claude/bin/pr-evidence`.
Public so images render inline in private-repo PRs (GitHub's image proxy cannot auth).
Do not commit anything but images and this file.
